# 📦 Inventory Management System

Full-stack project built using **Express.js**, **Sequelize**, **PostgreSQL**, **Next.js (App Router)**, **TailwindCSS**, and **shadcn/ui**.

This system manages:

- Warehouses  
- Products  
- Stocks  
- Purchase Requests  
- Purchase Request Items  

---

## 🚀 How to Run the Project Locally

---

# 1. Backend (Express.js + Sequelize + PostgreSQL)

### **Prerequisites**

- Install **Node.js** (v18+ recommended)  
- Install **PostgreSQL**  
- Create a database:

```sql
CREATE DATABASE foomlab_dev;

# Clone the repository
git clone <repo-url>
cd backend

# Install dependencies
npm install

# Copy environment variables
cp .env.example .env
# Edit .env with your PostgreSQL username/password

# Run migrations
npx sequelize-cli db:migrate

# Start the server
npm run dev


backend/
│── src/
│   ├── models/        # Sequelize models
│   ├── migrations/    # DB migrations
│   ├── services/      # Business logic & querying DB
│   ├── controllers/   # Handle requests, validations, responses
│   ├── routes/        # Route definitions
│   └── server.js      # App entry point (Express, CORS, JSON parsing)
│
└── .env.example

# Database Schema

The tables were created manually in PostgreSQL (not auto-generated by Sequelize).
Sequelize ORM maps to these existing tables.
```
CREATE TABLE warehouse (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL
);

CREATE TABLE product (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    sku VARCHAR(50) NOT NULL UNIQUE
);

CREATE TABLE stock (
    warehouse_id INT NOT NULL,
    product_id INT NOT NULL,
    quantity INT NOT NULL DEFAULT 0,
    PRIMARY KEY (warehouse_id, product_id),
    FOREIGN KEY (warehouse_id) REFERENCES warehouse(id) ON DELETE CASCADE,
    FOREIGN KEY (product_id) REFERENCES product(id) ON DELETE CASCADE
);

CREATE TABLE purchase_request (
    id SERIAL PRIMARY KEY,
    reference VARCHAR(20) NOT NULL UNIQUE,
    warehouse_id INT NOT NULL,
    status VARCHAR(20) NOT NULL DEFAULT 'PENDING',
    FOREIGN KEY (warehouse_id) REFERENCES warehouse(id)
);

CREATE TABLE purchase_request_item (
    id SERIAL PRIMARY KEY,
    purchase_request_id INT NOT NULL,
    product_id INT NOT NULL,
    quantity INT NOT NULL,
    FOREIGN KEY (purchase_request_id) REFERENCES purchase_request(id) ON DELETE CASCADE,
    FOREIGN KEY (product_id) REFERENCES product(id)
);

# Backend Design Decisions

✔ Uses Sequelize ORM, but does NOT auto-create tables because the schema already exists
✔ Uses a service layer to keep DB queries clean and reusable
✔ Uses controllers to handle:

# business logic
- validations
- returning success/error responses
- HTTP status codes (200, 400, 500)

✔ Routes are separated by resource for clarity
✔ server.js configures Express, CORS, JSON parsing, and routes

# Relations Used

## One-to-many:

PurchaseRequest → PurchaseRequestItems
A purchase request can contain multiple items.

Many-to-many (implemented via composite key):

Stock = (warehouse_id, product_id)

This structure makes the backend clean, scalable, and easy to maintain.


# 2. Frontend (Next.js + TailwindCSS + shadcn/ui)
# Clone the repository
git clone <repo-url>
cd frontend

# Install dependencies
npm install

# Start development server
npm run dev

Frontend Tech Choices

✔ shadcn/ui — consistent modern component library
✔ TailwindCSS — fast styling workflow
✔ Next.js App Router — clean folder-based routing

Frontend Routing Structure
/purchase-requests
    /new        # Create new purchase request
    /[id]       # Edit an existing purchase request

💡 Design Decisions Summary
Backend

Sequelize ORM to map existing SQL tables

No auto-sync; manual SQL schema

Layered structure: Services → Controllers → Routes → server.js

Controller validations

Nested responses (e.g., purchase request includes items)

Frontend

Built with Next.js (App Router)

Styled using TailwindCSS + shadcn/ui

Fetches live warehouse & product lists

Supports editing purchase request items dynamically

🚧 Possible Improvements
🔐 Backend

Add JWT authentication

Add role-based authorization

Improve input validation

Add audit logs

🖥️ Frontend

Add authentication (JWT)

Add form validation

Add pagination & search

📈 System

Add logging (Winston/Morgan)

Add Docker support

Add unit tests (Jest / Supertest)